  type User {
    id: ID! @unique 
    username: String!
    email: String!
    uid: String
    tags: Tag
  }

  type UserTag {
    id: ID! @unique
    user: User!
    tag: Tag!
  }

  type Tag {
    id: ID! @unique
    name: String! @unique
  }
